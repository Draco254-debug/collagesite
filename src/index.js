// index.js
import 'bootstrap/dist/css/bootstrap.css'; // Import the Bootstrap CSS file
import React from 'react'; // Import the React library
import ReactDOM from 'react-dom'; // Import the ReactDOM library
import App from './App'; // Import the App component
import './index.css'; // Import the CSS file for the project
// Render the App component to the root element of the document
ReactDOM.render(<App />, document.getElementById('root'));
